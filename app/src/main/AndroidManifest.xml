<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/imagecart"
        android:label="@string/app_name"
        android:roundIcon="@drawable/imagecart"
        android:supportsRtl="true"
        android:theme="@style/Theme.Userstore">
        <activity android:name=".ui.ShopingCart">


        </activity>
        <activity android:name=".ui.ProductDetails"></activity>
        <activity android:name=".Splach" />
        <activity android:name=".MainActivity">

        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".authentication.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".authentication.registration" />
        <activity android:name=".authentication.ForgetPssword" />
        <activity android:name=".ui.EditSetting" />
    </application>

</manifest>